# Design Audit

## Scope

対象ページ:

- `/`
- `/foods`
- `/foods/[id]`
- `/eaten`
- `/areas`

参照した観点:

- Typography
- Layout
- Cards
- Buttons
- Spacing
- 写真優先の情報設計
- 管理画面感の排除

参考資料は構成と設計観点のみ参照し、文章、画像、ロゴ、ブランド表現はコピーしていません。

## Overall Finding

現在のUIが悪く見える主因は、情報量そのものではなく、情報の見せ方です。

このアプリはUSJフード図鑑なのに、画面上では「数字」「ステータス」「フィルター」「カード枠」が写真と同じ強さで主張しています。その結果、旅行・記録・コレクションのアプリではなく、食品データを管理する内部ツールのように見えます。

## Why It Looks Admin-ish

### 1. すべてを白いカードで囲っている

ホーム、食べた、検索、詳細の多くの情報が `rounded`, `border`, `bg-white`, `shadow` のカードで処理されています。カード自体は悪くありませんが、すべての情報をカード化すると、重要度の差がなくなります。

結果:

- 写真より枠が目立つ
- セクションごとの差が弱い
- UIテンプレート感が出る
- 画面全体が「箱の集合」に見える

改善:

- セクションは余白と見出しで分ける
- 本当にカード化するのは商品、エリア、入力フォームだけにする
- 統計や注釈はカードではなく行・ゲージ・小さなテキストにする

### 2. 数字が写真より強い

図鑑アプリの主役は商品写真です。しかし現在はコンプ率、件数、表示数、確認状態などの数字が目立ちやすく、写真と競合しています。

結果:

- ユーザーが食べ物を見に来たのに、管理数値を読まされる
- 「集めたい」より「管理されている」印象が強い
- フードアプリではなくダッシュボードに見える

改善:

- 数字は進捗バーに添える
- 大きな統計カードを減らす
- 写真の下に商品名、価格、エリアを置く
- コンプ率は補助的に見せる

### 3. バッジが多すぎる

商品カードには限定、価格確認済み、販売状態、おすすめ、人気など複数の状態が乗りやすい構造になっています。

結果:

- サムネイルが汚れる
- App Storeのスクリーンショットで安っぽく見える
- 状態表示が多すぎて、商品自体の魅力が弱まる

改善:

- 画像上のバッジは最大2個
- 価格確認系ラベルは画像上に出さない
- 状態表示はカード本文下部か詳細ページに移す

### 4. フィルターUIが内部管理画面に近い

`/foods` の検索・フィルター領域は、カテゴリ、エリア、価格、販売状態、表示件数、ビュー切替などがまとまっていて、一般ユーザーには操作前の負荷が高いです。

結果:

- 商品を見る前に管理UIを見せられる
- 探す画面が「一覧管理」っぽくなる
- モバイルでファーストビューがフィルターに食われる

改善:

- 初期表示は写真一覧を優先
- カテゴリとエリアは横スクロールチップ中心にする
- 詳細フィルターは折りたたむ
- 表示件数や画像付き件数などの内部統計は通常画面から外す

### 5. カードの中にカードがある

ホームや食べたページでは、セクションカードの中に小さなカードやミニ統計が入る構造が多く見られます。

結果:

- 視線の抜けがなくなる
- 余白が重くなる
- どこを押せばよいか分かりづらい
- AI生成UIのような「それっぽい箱」の集合に見える

改善:

- セクション自体をカードにしない
- 商品カード、エリアカード、フォームだけを明確なカードにする
- 進捗は細いゲージやリストにする

## Page Audit

## `/` Home

良い点:

- 図鑑サマリー、25th、エリア導線へ寄せた方向性は正しい
- 写真を使った25thコレクションやエリア導線はアプリの本質に合っている
- おすすめランキング中心から離れたことは良い

問題:

- 進捗セクションがまだ「大きな管理カード」に見えやすい
- 25thが数字カードとして見えると図鑑感よりダッシュボード感が出る
- `FoodRequestPrompt` が白いボックスとして強く出ると、ホーム上部の雰囲気を壊す
- エリア進捗も数値が主張しすぎると旅行感より管理感が強くなる

改善案:

- ファーストビューは写真または柔らかい図鑑ヘッダーと細い進捗バーにする
- 25thは統計ではなくサムネイルコレクションにする
- 情報提供CTAは小さく、補助リンクとして扱う
- ホーム上の数値は「現在販売中 194 / 食べた 1 / 残り 193」程度に絞る

## `/foods`

良い点:

- 商品カードの高さ固定や画像比率統一の意識がある
- 価格、エリア、販売状態を一覧で見せようとしている
- 現在販売中と図鑑すべての切り替えがある

問題:

- 検索・フィルター領域が業務システムに近い
- カード内の状態表示が多く、写真が主役になり切れていない
- 価格確認済み、販売状態、限定などの表示が同じ強さで並びやすい
- 固定高カードは安定する一方、情報を詰めすぎると窮屈に見える

改善案:

- 初期画面は商品写真グリッドを先に見せる
- フィルターはチップ + 詳細条件折りたたみにする
- カード本文は商品名、価格、エリア、食べた操作に絞る
- 画像上のバッジは限定、新着、販売終了注意だけにする
- 店舗名や確認情報は詳細ページへ移す

## `/foods/[id]`

良い点:

- 代表画像1枚中心に寄せる方向性は正しい
- 価格、販売状態、販売期間、販売場所、レビュー、修正報告が揃っている
- 修正報告フォームが詳細下部にあるのは公開アプリとして良い

問題:

- 販売期間、販売場所、レビュー、確認情報、修正報告がすべてカード化されると縦に重い
- 詳細情報が多く、最初に見るべき情報の階層が弱くなる
- 確認情報やsource系が強く出ると管理画面感が出る

改善案:

- 上部は画像、商品名、価格、販売状態、販売場所、食べたボタンだけに絞る
- 確認情報は小さな補助セクションまたは折りたたみにする
- 修正報告は目立ちすぎず、しかし見つけやすい下部導線にする
- 関連商品は最大1-2セクションに整理する

## `/eaten`

良い点:

- 食べたページを自分の記録に寄せる方向性は正しい
- エリア別・ジャンル別の細いゲージはコンプ図鑑に合っている
- データ管理を下部または設定に逃がす方針は良い

問題:

- サマリーが数字カードになると管理画面に戻りやすい
- エリア別、ジャンル別、最近食べた、商品一覧がすべてカードで並ぶと密度が重い
- FoodGridをそのまま食べた一覧に使うと、検索ページの管理感が混ざる

改善案:

- 上部サマリーは2本の進捗バーと最小限の数字にする
- エリア別・ジャンル別は小さなゲージリストにする
- 最近食べたものは写真ストリップにする
- 食べた商品一覧はフィルター過多にせず、記録一覧として見せる
- 集計の考え方は最下部の小さな注釈にする

## `/areas`

良い点:

- エリア画像を使ったカードは、このアプリの中で最も旅行アプリに近い
- エリア別に食べた/未食を見せる発想は実用的
- 写真背景とエリア名の組み合わせはUSJ体験に合う

問題:

- 画像上の統計が増えすぎると文字が読みにくくなる
- エリアカード内で候補商品名や説明文を出すと重くなる
- エリア詳細がリストと統計だらけになると管理画面に戻る

改善案:

- エリアカードは画像、エリア名、食べた数、販売中数、コンプ率、未食数までに絞る
- 黒グラデーションを強め、文字可読性を最優先する
- 詳細では食べた一覧、未食一覧、販売終了一覧を分ける
- 候補商品名はホームのエリアカードには出さない

## Priority Fixes

### P0: `/foods` の管理画面感を減らす

最も一般ユーザーが触るページなので、フィルターUIとカード情報量を減らします。

実施内容:

- チップ中心の検索導線
- 詳細フィルター折りたたみ
- カード内情報を写真、名前、価格、エリアに絞る
- 画像上の価格確認ラベルを削除

### P1: ホームを写真図鑑らしくする

進捗は必要ですが、数字カードではなく、写真とゲージで見せます。

実施内容:

- 図鑑サマリーを1つの進捗セクションにまとめる
- 25thをサムネイルコレクション化する
- エリアカードは画像と少数の進捗だけにする

### P2: `/eaten` を記録ページとして軽くする

データ管理や過剰な補助導線を外し、自分の食べた記録に集中させます。

実施内容:

- データ管理は設定へ
- 集計注釈は最下部
- エリア/ジャンル進捗は小型ゲージ
- 食べた一覧は検索ページと別の見え方にする

### P3: 詳細ページの縦長さを抑える

商品詳細は見る、記録する、報告するに絞ります。

実施内容:

- 代表画像1枚
- 価格と販売期間を上部に整理
- 確認情報は控えめに
- 修正報告フォームは下部に自然配置

## Concrete Reasons The Current UI Feels Bad

1. 写真よりもカード枠、影、バッジ、数字のほうが目立つ。
2. フードアプリなのに、表示件数や確認状態など管理者向け情報が見えている。
3. どのセクションも白い角丸カードで処理され、画面にリズムがない。
4. 商品カードに状態ラベルが多く、サムネイルの魅力を邪魔している。
5. フィルターやセレクトが多く、ユーザーに「探す」より「条件管理」をさせている。
6. 進捗が大きな数字カードで表現され、コレクション感よりダッシュボード感が出ている。
7. 詳細ページが複数のボックスの縦積みになり、商品そのものの印象が弱くなる。
8. 旅行・図鑑・記録・管理のトーンが混ざっており、プロダクトの人格がぶれている。

## Target State

最終的なUIは、次の状態を目指します。

- ホームは図鑑の現在地が分かる
- 商品一覧は写真で選べる
- 商品詳細は見る、記録する、報告するだけに集中している
- 食べたページは自分の記録として気持ちよく見られる
- エリアページは旅行アプリのように場所から選べる
- 管理情報は管理画面か下部の補助情報に閉じている

この状態になれば、アプリは「USJフード情報サイト」ではなく、「USJフード図鑑・記録アプリ」として見えるようになります。

