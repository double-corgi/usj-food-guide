# design-review-i18n-phase2c-a.md

## レビュー対象

- 指示書: `docs/codex-goal-i18n-phase2c-a.md`
- バックアップコミット: `957a711`（backup-before-i18n-phase2c-a）
- 実装コミット: `6d6df87`（implement-i18n-phase2c-a-foods）
- レビュー方法: `git diff 957a711 6d6df87 --stat` / 全差分read + 本番環境 (`https://new-app-chi-rosy.vercel.app`) への独立fetch + `npm run lint`実行

変更ファイル: `app/foods/page.tsx`, `components/food-card.tsx`, `components/food-grid.tsx`, `lib/i18n/dictionaries.ts`（4ファイル、+261/-68）

---

## 1. `/foods`の固定UI文言だけが多言語化されているか

**確認済み。** `components/food-grid.tsx`の主要文言（見出し「フードを探す」/サブタイトル/検索placeholder/「表示条件」/カテゴリチップの「すべて」/件数表示/0件時表示/CTA文言）、フィルター・並び替えの`<select>`オプション全項目、`TogglePill`全項目が`t()`/既存キー再利用に置き換えられている。`app/foods/page.tsx`の`title="フードを探す"`ハードコードも削除され、`food-grid.tsx`側の`t("foods.title")`に統一されている。

`lib/i18n/dictionaries.ts`には`foods.*`/`foodCard.*`名前空間が新設され、ja/en/ko/zh-TWの4言語すべてに値が追加されている（型エラーなし、`npm run lint`成功を確認）。

指示書にない追加キー`foods.kicker`（「フード図鑑」のキッカー文言）も4言語分追加されており、これは指示書の「主要文言」の範囲に自然に含まれる適切な追加と判断する。

---

## 2. `/foods/[id]`に踏み込んでいないか

**確認済み。** `git diff --stat`で`app/foods/[id]/page.tsx`、`components/food-detail.tsx`は変更されていない。`components/food-card.tsx`の変更（2.3で許可された範囲）は`/foods/[id]`の関連商品カードにも影響するが、これは指示書で明示的に許容された共有コンポーネントの変更であり、`food-detail.tsx`自体の文言（一覧へ戻る/確認情報等）には手を入れていない。

---

## 3〜6. 商品名・店舗名・エリア名・カテゴリ名・ジャンル名が翻訳されていないか

**確認済み。** 本番`/foods`（ja）をfetchし、以下を確認した。

- 商品カテゴリ名（チュリトス/ポップコーン/ドリンク/ピザ/バーガー/パスタ/プレート/ライス/キッズ/スイーツ、および`categoryLabels`の「チキン・肉系」「ライス・カレー」「麺・パスタ」「スナック」「季節限定」「セットメニュー」等）は日本語のまま
- エリア名（スーパー・ニンテンドー・ワールド/ミニオン・パーク/ハリウッド・エリア等）、店舗名（キノピオ・カフェ/三本の箒等）は日本語のまま
- `shopTypeLabels`（レストラン/フードカート/ワゴン/未分類）、`diningTypeLabels`（テイクアウト/店内飲食等）、`statusLabels`（公開情報確認中/復刻待ち等）も未変更

`food-card.tsx`の差分で確認した通り、`getSaleUrgencyLabel(food)`の戻り値（`lib/food-utils.ts`由来）は翻訳対象から除外されており（指示書2.3(b)の判断どおり）、商品名・データ由来文言への侵食は見られない。

---

## 7. URL構造が変わっていないか / 8. `/en` `/ko` `/zh-TW`が追加されていないか

**確認済み。** `/foods`のURLパス・構造に変更なし。`https://new-app-chi-rosy.vercel.app/en/foods`をfetchした結果、コンテンツが返らず（404相当）、ロケール別パスは追加されていない。

---

## 9. ホームv1.2が壊れていないか / 10. area-detail-v1.1が壊れていないか

**確認済み（範囲内）。** 本番`/`をfetchし、ヒーロー、コレクション概要、エリア一覧、フッター等、Phase2B/2B.1で確認済みの構成・文言がすべて維持されていることを確認した。

`food-card.tsx`の変更は`/`・`/areas/[id]`のカード表示にも影響するが、変更内容はバッジラベル（「限定」「販売終了」「近日販売」）とボタン文言（「食べた」/「食べた済み」）・価格未確認表示の文言を`t()`に置き換えただけで、構造・クラス・レイアウトの変更はない（diff確認済み）。ja環境では表示文言が完全に一致するため、ja表示上はホーム・エリア詳細の崩れは生じない。

ただし、**en/ko/zh-TWでのホーム・エリア詳細・`/eaten`上のカード表示（特にバッジ・ボタンの折り返し）は本レビューでは未確認**（13章参照）。

---

## 11. ja/en/ko/zh-TWで表示崩れがないか / 12. 390px/430pxで横スクロール・文字切れがないか

**ja環境のみ確認済み。** 本番`/foods`（ja, デフォルトロケール）をfetchし、レイアウト・全文言が正しく表示されることを確認した。

**en/ko/zh-TWでの`/foods`表示、および390px/430pxでの確認は、本レビューでは実施できていない。** `docs/`配下に本Phaseのスクリーンショット等のエビデンスファイルが見当たらず、Codexの最終報告内容（提供されていれば）との照合もできていない。Phase2B/2B.1では同様の検証にスクリーンショットが提出されており、本Phaseでも同様のエビデンスが必要。

---

## 13. 追加で見つかった懸念事項（指示書のチェック項目には無いが重要）

### 13.1 `foodCard.eatenDone`と`common.eaten`の訳が言語によって区別できていない

`food-card.tsx`のボタンは次のように実装されている。

```tsx
{eaten ? t("foodCard.eatenDone") : t("common.eaten")}
```

ja（「食べた」/「食べた済み」）では「これから食べる」と「すでに食べた」が文言で区別できるが、追加された訳のうち以下は**両状態が同じ表示文言になってしまう**。

- **en**: `common.eaten` = `"Eaten"`、`foodCard.eatenDone` = `"Eaten"` → 未食/食べた済みのボタンが両方とも"Eaten"と表示され、ユーザーは現在の状態をボタン文言から判別できない。
- **zh-TW**: `common.eaten` = `"已吃"`、`foodCard.eatenDone` = `"已吃"` → 同上。

ko（`common.eaten`=「먹은 것」/ `foodCard.eatenDone`=「먹음」）は語として異なるが、いずれも「食べた（過去）」の意味合いが強く、未食状態を押すボタンとして適切かは要検討。

これは単なる訳語の質の問題ではなく、**「食べた/未食」を切り替えるトグルボタンの状態がen/zh-TWで視覚的に判別不能になる**機能上の問題であり、ユニコレの「ゲーム感」「コレクション欲」（食べた/未食の状態が一目でわかること）にも関わる。

### 13.2 build未実施（本レビュー環境の制約）

本レビュー環境では`npm run build`がサンドボックスのネットワーク制限（`registry.npmjs.org`に到達不可、`@next/swc-linux-arm64-gnu`のダウンロード失敗）により実行できず、build成功を独立に確認できなかった。`npm run lint`は成功している。Codexの最終報告でのbuild成功報告に依拠する。

---

## 総合判定

**条件付き承認**

### 条件

1. **en/ko/zh-TWでの`/foods`表示確認（390px/430pxを含む）**: 本番環境でのスクリーンショット等のエビデンスを提出すること。特にフィルター`<select>`・`TogglePill`群の文言が長くなることによる折り返し・はみ出しがないかを確認する（`docs/i18n-phase2c-design-v1.md` 5章 リスク1・2に該当）。
2. **`foodCard.eatenDone`/`common.eaten`の訳の見直し**: en（"Eaten"/"Eaten"）・zh-TW（已吃/已吃）で未食・食べた済みの状態がボタン文言から区別できない問題を修正すること。例: en側は未食ボタンを"Mark as Eaten"等の能動的な表現にし、食べた済み側を"Eaten"とするなど、状態が視覚的に区別できる訳に変更する（ko含め4言語で再確認）。
3. **`/`・`/areas/[id]`・`/eaten`でのen/ko/zh-TW表示確認**: `food-card.tsx`変更がこれらのページのカード表示（バッジ・ボタン）に与える影響を、最低限スクリーンショットで確認すること。

範囲遵守（`/foods`限定、`/foods/[id]`未着手）、URL方針、商品名・店舗名・エリア名・カテゴリ名・ジャンル名の非翻訳、ja表示の正しさについては問題なし。

まだPhase2C-Bの`/goal`は作成しない。
