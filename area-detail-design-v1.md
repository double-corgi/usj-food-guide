# Area Detail Design v1 — /areas/[id] 再設計書

作成日: 2026-06-13
作成者: Claude（プロダクト責任者 / UX / UIデザイナー）
対象: `/areas/[id]` のみ。`/areas` 一覧・ホーム・他ページは触らない。
基準: ホームv1.2（承認済み）を世界観の正とする。ホーム自体は再改修しない。
前提資料: app-wide-design-review-v1.md（P1指摘）/ 現行実装の全文確認済み（app/areas/[id]/page.tsx 139行・area-collection-summary.tsx・area-eaten-foods.tsx・area-food-status-lists.tsx・area-overview.tsx）/ スクリーンショット（area-detail-home-polish-after-390/-1280.png、拡大クロップ含む）

本書は設計のみ。コードは書かない。Codex用 /goal は別途。

---

## 1. Objective

エリア詳細を「エリアの管理票」から「**エリアのコレクション帳の1ページ**」に変える。

達成基準:

- `/areas` 一覧の美しいポスターをタップしたとき、**同じ写真の世界が続く**（着地の裏切りをなくす）
- 開いて3秒で「このエリアであと◯品」「まず何を食べるべきか」が分かる
- 統計カード・英語ラベル・チップ壁が消え、ホームv1.2と同じ温度（ウォーム紙・Gold/Navy・カードレス）になる
- 食べた記録がこのページでも「色づいたタイル＋Goldスタンプ」として誇らしく見える（取得表現の統一言語化）

## 2. Current Problem

スクリーンショットとコードで確認した現状の問題:

**なぜ古く見えるか** — ページ全体が旧世代の文法で組まれている。紺色ヒーロー（`bg-ink`）＋白カード＋`shadow-soft`＋グラデ販促カード（amber/rose）＋埋め込みFoodGridという「カードの縦積み」で、ホームv1.2が捨てた様式が全部残っている。

**なぜ管理画面っぽいか** — (a) ヒーロー直下に「フード37 / 販売場所15 / 期間限定4 / カート販売7」の統計カード4連発（設計ルールの明示的禁止事項）。(b) 販売場所がグレーチップ15個以上の壁。(c) 「Area Memory」「Missing Foods」「Archive Foods」のuppercase英語ラベル（装飾英語の禁止事項）。(d) ページ末尾に検索フィルタ付きFoodGridが丸ごと埋め込まれ、詳細ページの中に一覧管理画面が同居している。

**なぜ「行きたい・食べたい」に繋がらないか** — (a) `/areas` 一覧はポスター写真が主役なのに、詳細に入った瞬間**エリア写真がゼロ**になり、紺のパネルと数字で迎えられる。(b) 「注目フード3品」に#1/#2/#3のランキングバッジが付き、発見の喜びではなく順位表に見える。(c) 数字（37品・15か所）が先、ごちそうの顔が後、という情報順序が逆。

## 3. Design Principle

ホームv1.2から引き継ぐもの:

1. **ウォーム紙背景**（#fffdf9 / #fffaf5）。紺ヒーロー・クールグレーは廃止
2. **Gold / Navy** の2色運用。amber/roseのグラデ販促カードは廃止
3. **カードレス**。セクションは余白と見出しで区切る。白カード＋shadowの量産をやめる
4. **写真が主役**。ヒーローはエリアポスター、本文はフードタイル
5. **統計ではなく「あと◯品」**。数字は1つだけ大きく、母数は「販売中◯品（登録分）」の非断定表現
6. **取得の統一言語**: 食べた=フルカラー＋Gold細枠＋Goldスタンプ（✓）、未食=微抑制（saturate 0.88 / brightness 1.03）。ホームの棚と同じCSS規則を流用
7. **空状態はゼロを突きつけない**。誘い文言1行
8. **英語装飾ラベル・絵文字・順位バッジを使わない**

## 4. Page Information Priority

新しい優先順位（上から）:

1. **エリア写真＋エリア名**（ヒーロー。世界観の継続）
2. **このエリアのコレクション状況**（あと◯品・食べた◯/販売中◯品（登録分）・細ゲージ。ヒーローに統合）
3. **まず食べたい**（非順位の代表3品。「何を食べるべきか」への即答）
4. **期間限定・終了間近**（行く理由の作成。該当があるときのみ）
5. **このエリアで食べたフード**（自分の記録。色づいたタイル）
6. **残りのフード**（今集められる未食一覧。タイルグリッド）
7. **販売終了フード**（折りたたみ。図鑑の記録として）
8. **販売場所**（補助情報に降格。折りたたみ＋店舗詳細への導線）

現行の「統計4項目」「埋め込みFoodGrid」はこの優先順位に存在しない（廃止）。

## 5. Hero Design

- **画像**: `area-overview.tsx` にハードコード済みの公式エリア画像10枚（`allowedAreas` 配列）を再利用する。この画像マップを共有モジュール（lib/constants.ts または lib/area-images.ts）へ移し、一覧と詳細の両方から参照する。**新規画像生成・合成画像は禁止**
- **構成**: モバイルはフルブリード（-mx-4）で高さ約240px。写真の上に `/areas` 一覧カードと同じ黒グラデーション（下方向に強く）を敷き、左下にエリア名（text-2xl〜3xl・白・font-black）。一覧カードの拡大版として知覚されるよう、グラデ・文字配置の文法を一覧と揃える
- **銘板の統合**: ヒーロー写真の直下（紙背景上）に6章のコレクション銘板を置く。写真の中に数字を載せすぎない（一覧カードとの差別化: 一覧は「残り◯品」を写真内に、詳細は写真外の銘板で詳しく）
- **戻る導線**: ヒーローの上に小さな「← エリア一覧へ戻る」テキストリンク（現行の白カードボタンより軽く）
- **フォールバック**: 画像マップに無いエリア（許可10エリア外）は、ウォーム紙背景＋Navyのエリア名＋Gold罫の「文字ヒーロー」（ホームのロゴ銘板構図を流用）。紺パネルには戻さない
- safe-area: ページ先頭要素になるため、ホームv1.2と同じ `max(env(safe-area-inset-top), 1rem)` 系の処理をヒーローに適用する

## 6. Area Collection Design（統計4連発の代替）

統計カード4連発（フード/販売場所/期間限定/カート販売）は**全廃**。代わりにヒーロー直下へ1行の銘板:

- 主役: 「**このエリアであと {uneaten}品**」（数字のみ大きく28〜32px・Navy。ページ内で大きい数字はこれ1つ）
- 従属: 「食べた {eaten} / 販売中 {total}品（登録分）」（12〜13px・slate）
- 直下に高さ4pxの細ゲージ（Blue→Goldグラデ。ホームと同一トークン）
- 全取得時: 「このエリアはコンプリート」＋Goldスタンプ表現（ホームの期間限定コンプスタンプと同一意匠）
- 計算は既存 `AreaCollectionSummary` のロジック（canonical・isCompletable基準）をそのまま使い、**見た目だけ**を紺カードから紙上の銘板に変える
- ホームの棚のミニ流用は**しない**（ページが長くなるため。取得表現は7章のタイルグリッド側で統一言語として表現する）
- 「限定 ◯品」の独立表示は廃止し、期間限定セクション（7章）の「あと◯品」チップに役割を移す

## 7. Food Sections

すべてカードレス（見出し＋余白で区切る）。タイルはホームの取得言語（食べた=カラー＋Gold枠＋✓スタンプ24px / 未食=微抑制）を適用する。

**(1) まず食べたい**

- 現行の `rankFoodsByStrategy(…, "first-visit", …, 3)` の選定ロジックは維持してよいが、**#1/#2/#3バッジは廃止**。見出しは「まず食べたい3品」、サブテキスト「このエリアで見つけるならここから。」
- 表示: 横並び3枚（モバイルは横スワイプ可）。写真4:3・商品名2行・価格。順位を示す数字・メダル・色分けは一切付けない

**(2) 期間限定・終了間近**（該当があるときのみ表示）

- 現行の「終了間近」セクションを統合・簡素化: 見出し横に「あと◯日」chipのみ。rose/amberグラデ背景カードは廃止し、紙背景に直置き
- 「残り◯日」は写真下のテキストで表示（写真上に載せない。ホームのレールと同じ規則）

**(3) このエリアで食べたフード**

- 「Area Memory」ラベル廃止。見出し「このエリアで食べたフード」のみ
- 表示: 正方形タイルのグリッド（モバイル3列）。食べたタイルはフルカラー＋Gold枠＋スタンプ。回数・日付は詳細に任せ、タイルを汚さない（最大8枚＋「すべて見る → /eaten」）

**(4) 残りのフード**

- 「Missing Foods」ラベル廃止。見出し「残りのフード」＋「あと◯品」チップ（Gold系。ホームの期間限定チップと同意匠）
- 表示: 正方形タイルのグリッド（モバイル3列・最大12枚）。未食の微抑制トーン。タイル下に商品名2行＋価格のみ
- 12枚を超える分は「残りをすべて見る → /foods?area={id}&sale=active&sort=uneaten」のテキスト行導線
- **ページ末尾の埋め込みFoodGrid（検索・フィルタUI付き）は廃止**。探す体験は /foods に一本化する（詳細ページ内に管理UIを持ち込まない）

**(5) 販売終了フード**

- 「Archive Foods」ラベル廃止。「販売終了フード（◯品）」の折りたたみ（details維持）は良い構造なので踏襲。日本語のみ・白カード枠は外し罫線区切りに
- grayscale処理はせず、未食と同じ微抑制＋「販売終了」テキストで区別（「沈めずに差を作る」原則）

## 8. Store / Location Treatment

チップ15個の壁を廃止し、**折りたたみの店舗リスト**に変える:

- 見出し「販売場所」＋「{shops.length}か所」の小表示
- 初期表示は主要6件のみ: 店舗名（`/stores/[id]` があればリンク）＋種別（レストラン/カート等）を1行ずつ、罫線区切りのリストで（商品詳細の販売場所リストと同じ文法）
- 7件目以降は `details` 折りたたみ「すべての販売場所を見る（あと◯か所）」
- チップ（角丸グレー錠剤）ではなく行リストにする理由: 店舗はタップして店舗詳細へ行く対象であり、装飾タグではないため
- 「店舗未確認」「エリア確認中」に該当する名称はこのリストに出さない（既存のフィルタ慣行を踏襲）

## 9. Empty States

- **食べた記録がない**: セクションを空ボックスで残さず、見出し＋1行「このエリアの1品目を見つけよう。」＋「残りのフード」への視線誘導（ゼロや0%を表示しない）
- **販売中フードがない**（ウォーターワールド等、残り0品のエリア）: 銘板を「このエリアの販売中フードは現在確認中です」＋「販売終了フード（◯品）」の折りたたみを主コンテンツに昇格。空のタイルグリッドを出さない
- **ヒーロー画像がない**: 5章のフォールバック（紙＋Navy文字ヒーロー）。プレースホルダSVGや灰色パネルを出さない
- **まず食べたい候補が3品未満**: あるだけ表示。0品ならセクション非表示（nullリターン）

## 10. Mobile Layout（390px / 430px）

上から:

| 要素 | 高さ目安 |
|---|---|
| 戻るリンク行 | 約40px |
| ヒーロー写真（フルブリード・黒グラデ・エリア名） | 約240px |
| 銘板（あと◯品＋ゲージ） | 約88px |
| まず食べたい3品（横スワイプ） | 約280px |
| 期間限定・終了間近（該当時） | 約260px |
| このエリアで食べたフード（3列タイル） | 内容次第 |
| 残りのフード（3列タイル・最大12） | 内容次第 |
| 販売終了（折りたたみ・閉） | 約56px |
| 販売場所（リスト6件＋折りたたみ） | 約320px |

- 1画面目（〜750px）で「写真＋エリア名＋あと◯品＋まず食べたいの先頭」まで見えること
- 横スクロールは「まず食べたい」「期間限定」の2レールのみ。タイルグリッドは縦
- セクション間は32〜48px。下部ナビとのクリアランスは既存ページ標準（pb-28相当）を維持
- 390/430とも横overflow禁止・文字切れ禁止

## 11. Desktop Layout（1280px / 1920px）

- コンテンツ最大幅1080px（ホームと同じ）。1920pxでは中央寄せで拡大しない
- ヒーロー: フルブリードをやめ、角丸（rounded-[2rem]）のワイド画像（高さ約320px）＋写真右下 or 直下に銘板。lg以上では**写真の右側に銘板を横並び**にする2カラム（左62%写真 / 右38%銘板＋まず食べたいの見出し）も可。実装コストが高ければ「写真→銘板」の縦積みで良い（必須は1080px幅と角丸）
- まず食べたい: 3カラムグリッド
- タイルグリッド: 食べた/残りとも6カラム
- 販売場所: 2カラムのリスト
- スマホ拡大に見せないポイント: ヒーローの高さを幅に比例させない（max 320px）、タイルサイズをキャップ（max 96px角）

## 12. Do / Don't

**Do**:

- `/areas` 一覧のポスター画像をヒーローで再利用（画像マップの共有モジュール化）
- 「このエリアであと◯品」を唯一の大きい数字にする
- 取得済み=カラー＋Gold枠＋✓スタンプ / 未食=微抑制（ホームとCSS規則を共有）
- 見出しはすべて日本語。セクションは余白で区切る
- 「販売中◯品（登録分）」の非断定表現
- 販売終了は折りたたみで残す（図鑑の記録）
- safe-area-inset-top の処理（ホームv1.2と同方式）

**Don't（禁止）**:

- 統計カード4連発（フード/販売場所/期間限定/カート販売）
- 管理画面風UI・紺色ダッシュボードヒーロー・白カード量産
- 「Area Memory」「Missing Foods」「Archive Foods」等の英語装飾ラベル（uppercase tracking含む）
- #1 / #2 / #3 のランキングバッジ・順位表現
- 販売場所チップの壁
- 合成画像・新規画像生成・プレースホルダSVGヒーロー
- 「全商品」「全フード」「完全収録」
- 絵文字のUIアイコン使用（既存セクションに出る場合は文字のみへ）
- ロック / ? / 黒塗り / grayscale での未食・終了表現
- ページ内への検索フィルタ付きFoodGrid埋め込み
- `calculateCompletion` 等の集計ロジック変更
- localStorage schema の変更・データ削除
- `/areas` 一覧・ホーム・他ページの改修（画像マップの共有化に伴うarea-overview.tsxの参照変更のみ例外）

## 13. Codex Implementation Notes

実際に全文確認したファイルと触り方:

| ファイル | 触り方 | 根拠 |
|---|---|---|
| `app/areas/[id]/page.tsx`（139行） | 主改修。紺ヒーロー＋AreaStat×4を廃止しヒーロー写真＋銘板へ。amber/roseグラデカード2種を紙上セクションへ。末尾の `<FoodGrid …>` 埋め込みを削除し /foods への導線に置換 | 統計4連発・グラデカード・FoodGrid埋め込みの実装箇所 |
| `components/area-collection-summary.tsx`（39行） | ロジック維持・見た目刷新。紺カード前提（text-white/bg-white/10）を紙上の銘板（Navy文字＋細ゲージ）へ | 計算は正しいので再利用 |
| `components/area-eaten-foods.tsx` | 「Area Memory」削除（33行目）。行リスト型からタイルグリッド型へ。Gold枠＋スタンプの取得言語を適用 | 英語ラベルと旧様式の実装箇所 |
| `components/area-food-status-lists.tsx`（96行） | 「Missing Foods」（35行目）「Archive Foods」（51行目）削除。白カード→紙上セクション。タイルグリッド化。終了商品の表現を微抑制に | 同上 |
| `components/area-overview.tsx` | `allowedAreas` の画像マップを共有モジュールへ移動し、ここからは参照に変更（**見た目は変えない**） | 画像10枚のハードコード元（9〜52行目） |
| `lib/constants.ts` または新規 `lib/area-images.ts` | 画像マップの移動先 | 共有定数の置き場 |
| `app/globals.css` | 必要ならヒーロー用safe-area・タイル取得言語のクラス共有化のみ | ホームv1.2の `.home-collection-hero` 等と整合 |

触ってはいけない: `lib/food-utils.ts`（集計）、`lib/use-food-logs.ts` / `lib/local-user-data.ts`（schema）、`lib/repositories/*`（公開判定）、`components/food-grid.tsx` 本体（/foodsで使用中。エリアページからのimport削除のみ）、`lib/food-value-score.ts`（first-visit選定ロジックは維持。表示だけ変える）、ホーム関連コンポーネント。

補足: 取得タイルのCSS（微抑制値・Gold枠・スタンプ）はホーム実装と値を一致させること。可能なら共通クラス化して二重定義を防ぐが、ホーム側ファイルの変更は最小に留める。

## 14. Verification（実装後の確認項目）

コマンド:

- `npm run lint` / `npm run typecheck` / `npm run build` すべて成功
- `localhost:3000` 200 OK（dev serverは止めない）

確認ページ: `/areas` `/areas/[id]`（最低3エリア: 残り多数のエリア・残り0のエリア・画像マップ外エリア）`/` `/foods` `/eaten` `/stores`

確認幅: 390px / 430px / 768px / 1280px / 1920px

確認項目:

- overflow 0 / clipped 0 / 横スクロールなし（全幅）
- `/areas` 一覧が壊れていない（画像マップ共有化後も見た目不変）
- `/areas/[id]` がホームv1.2世代に近づいている: ウォーム紙背景・紺ダッシュボード消滅・統計4連発消滅
- 「Area Memory」「Missing Foods」「Archive Foods」がgrepで0件
- #1/#2/#3 バッジが存在しない
- 販売場所がチップ壁でなく折りたたみリストになっている
- ヒーローにエリア写真が表示される（画像マップ外エリアは文字ヒーローにフォールバック）
- 「このエリアであと◯品」が唯一の大きい数字
- 取得済みタイルにGold枠＋スタンプ、未食タイルが微抑制（茶色くくすんでいない）
- 空状態（記録なし/販売中なし）でゼロの壁・空ボックスが出ない
- 「全商品」「全フード」「完全収録」が無い
- エリアの食べた数・残り数がホーム/eatenの集計と矛盾しない（集計ロジック不変の確認）
- iOS Simulator（iPhone 14 Pro系）で上部safe-area干渉なし
- スクリーンショット保存: `area-detail-v1-after-390/430/768/1280/1920.png`＋Simulator版＋空状態版

---

オーナー承認後、本書を正本として codex-goal-area-detail-v1.md（実装指示書）を作成する。本書では作成しない。
