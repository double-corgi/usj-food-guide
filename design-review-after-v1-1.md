# Design Review After v1.1 — ユニコレ ホーム視覚修正レビュー

レビュー日: 2026-06-12
レビュー担当: Claude（プロダクト責任者 / UX / UIデザイナー）
対象: Codexによる docs/codex-goal-home-v1.1.md の実装
確認方法: **スクリーンショット精査を主、コード確認を従**とした。screenshots/home-design-v1-1-after-{390,430,768,1280,1920,eaten-390}.png および home-design-v1-1-logo-1280.png（拡大）を目視。期間限定コレクションの選出はデータシミュレーションで照合。Dynamic Island干渉はコード（safe-area処理の有無）で裏取りした。

本レビューは評価のみ。コード変更・実装・git操作は行っていない。

---

## 1. 総合判定: **条件付き承認**

v1.1の修正対象（ロゴ崩壊・くすみ棚・棚サイズ・0品状態・期間限定・スタンプ・文言・デッドコード）は**すべて修正されており、ブラウザ上のビジュアルは初めて合格水準に達した**。390pxのファーストビューは「色とりどりの棚＋誘い文言」になり、見た瞬間の印象が別物に良くなっている。

ただし、ご指摘の **Dynamic Island付近のロゴ切れは実在する構造問題であり、P0と判定する**。モバイルで safe-area-inset-top を処理している要素がひとつも無いことをコードで確認した。iOS（Capacitor/PWA standalone）での公開はこのP0を直すまで不可。ブラウザ表示のみなら現状で公開可能水準のため「条件付き承認」とする。

## 2. 問題分類

### P0: モバイルでロゴがDynamic Island / status barと干渉する（新規・スコープ外起因）

判定根拠:

- `app/layout.tsx` は `viewportFit: "cover"` を指定しており、standalone PWA / Capacitor ではコンテンツがstatus bar・Dynamic Islandの下まで描画される
- `safe-area-inset-top` を処理しているのは `components/app-header.tsx` のヘッダーのみで、それは `hidden md:block` — **モバイルでは存在しない**
- ホームの先頭要素（ロゴを含むhero）の上余白は `pt-4`（16px）。iPhone 14 Pro系の上部セーフエリアは約59pxのため、ロゴが島/時計の裏に潜る
- つまりブラウザでは正常、アプリ実行時のみ切れる。Codexの検証がブラウザスクリーンショットのみだったため検出されなかった

P0とする理由: このプロジェクトはiOS/Androidアプリ化が明示目標（ios/ディレクトリ、capacitor.config.ts、docs/app-store-listing-ja.md）。アプリの第一画面でブランドロゴが端末ハードウェアに切られるのは、v1.0のロゴ崩壊と同格のブランド毀損。

修正方向（次の/goal用・実装はしない）: heroセクションのモバイル上余白を `max(env(safe-area-inset-top), 16px)` 相当にする。なお同じ理由で**ホーム以外の全ページも** standalone時は先頭が島に近接する（layout mainのpt-6=24pxのみ）。ホーム外への波及確認をP1として併記する。

### P1: safe-area問題の他ページ波及（要実機確認）

`/foods` `/eaten` 等もモバイルで上部セーフエリア処理が無い。各ページ先頭は見出しテキストのため即死ではないが、iOSシミュレータでの全ページ確認が必要。v1.1スコープ外のため別対応とする。

### P2

1. **0品銘板の「コレクション数」ラベルが宙に浮く**: 0品時は数字を出さない仕様にしたため、「コレクション数」というラベルだけが値なしで表示されている（390/1280で確認）。ラベルごと隠して誘い文言だけにする方が自然
2. **「棚が色づく」の重複**: 棚上キャプション「食べると、棚が色づく。」と0品銘板「最初の1品を記録すると、棚が色づきます。」が同一画面に並び、同じ比喩が2回出る。0品時はどちらか一方に
3. **lint / typecheck / build・commit/push・Vercel反映が未確認**: Codexの最終報告書を受領していないため、本レビューでは検証コマンドの成否を確認できていない（指示書19項目の報告が必要）

### P3

1. **IP除外リストの将来リスク**: `PURE_IP_GROUP_NAMES` にヨッシー・ルイージ・ピーチ・ジョーズ・ワンピース等が未収載。現データでは「25周年」が選出されるため実害なしだが、データ次第でキャラ名グループが「期間限定コレクション」を名乗る余地が残る。限定性ワードを含むグループ名を優先する等の堅牢化を将来検討
2. **期間限定コレクションのタイトルが「25周年」と素っ気ない**: eventName生値の表示のため。「25周年アニバーサリー」等への整形は将来の磨き込みでよい
3. Next.js devインジケータが390スクリーンショット左下に写り込んでいる（本番ビルドでは消えるため指摘のみ）

## 3. v1.1で直すべきだった項目の達成確認（スクリーンショット根拠）

| 項目 | 判定 | 根拠 |
|---|---|---|
| P0-1 ロゴ崩れ | ✅ 修正 | logo-1280拡大で確認: text-shadow単発のGoldが文字に正しく追従。金の残骸・折り返し・文字への重なりは390/430/768/1280/1920すべてで消滅。疑似要素はコードからも削除済み |
| P0-2 未取得棚のくすみ | ✅ 修正 | `saturate-[0.88] brightness-[1.03]`（sepia廃止）。390/1280とも棚はカラフルで「茶色い壁」が解消。0品状態でも美味しそうに見える |
| P1-1 棚サイズ | ✅ 修正 | モバイル2段×4列=8枚。390で1枚ずつ料理が判別できる。md 2×8 / lg 3×6維持 |
| P1-2 棚の意味づけ | ✅ 修正 | 「食べると、棚が色づく。」が棚上に1行（ゴールドブラウン、控えめで品がある） |
| P1-3 PC左カラム間延び | ✅ 改善 | lg行揃えをitems-start化。左カラムは上に詰まり、1280/1920とも「計画された余白」に見える |
| P1-4 期間限定=ミニオン | ✅ 修正 | 実画面の選出は「25周年」（あと10品）。IP名除外・限定比率70%・上限24品・候補条件すべて実装。データシミュレーションでもミニオン/マリオ/ハローキティは除外確認 |
| P2-1 スタンプ | ✅ 修正 | 24px（h-6 w-6）＋Gold細枠ring-2＋弱い内側グロー。logo-1280拡大で取得タイルの誇らしさが明確 |
| P2-2 0品状態 | ✅ 修正 | %とゲージ非表示。「最初の1品を記録すると、棚が色づきます。」＋「販売中 183品（登録分）」。1品取得後は通常表示（1 / 残り182・1%）へ正しく切替（eaten-390） |
| P2-3 写真ばらつき | ✅ 範囲内対応 | shelfScoreに既存imageQualityScoreを導入。768の暗黒タイルは軽減（完全制御は元データ依存のため許容） |
| 文言 | ✅ 修正 | 「全商品を見る」→「登録済みコレクションを見る」。「全183品/全フード/完全収録」なし |
| ロック / ? / COLLECTION | ✅ 残存なし | UI要素としては不存在。※390の1枚目商品「ファイアフラワー×ハテナブロック」の商品写真自体に?ブロックが写っているが、これは実商品の写真でありUIの?マークではない（違反ではない） |
| スタンプ誤発火 | ✅ 修正 | `useNewlyStampedKeys` に ready ガード追加（コード確認） |
| グロー色 | ✅ 修正 | achievement-unlockが `rgba(253,187,48,…)` のGold系へ。ミントグリーン消滅 |
| デッドコード | ✅ 削除 | HomeHeaderStats / HomeProgressStatusClient / StatusMini はgrepで参照ゼロ・削除済み |
| 棚とレールの重複 | ✅ 修正 | nonShelfFoods優先で充足時は完全排他、不足時のみ補充するロジックを確認 |

## 4. スマホファーストビュー評価（390 / 430）

- **ブラウザ表示としては合格**。ロゴ→タグライン→キャプション→色とりどりの棚2×4→誘い文言の流れが自然で、1画面目に「今集められるフード」のピンクの大判カードが覗き、スクロール動機も機能している
- 棚の写真が大きくなり「ドリンクボトル」「プレート」「スイーツ」が判別できる。初見で「これを集めるアプリだ」が伝わる
- 銘板は0品でも寂しくない（ただしP2-1/P2-2の文言調整余地あり）
- 下部ナビとの干渉なし
- **ただしDynamic Island干渉（P0）はアプリ実行時に発生するため、シミュレータ確認なしに「FV合格」とは言えない**

## 5. PC版評価（1280 / 1920）

- スマホ引き伸ばしには見えない。左=ブランド＋銘板 / 右=棚3×6の2カラムが成立し、v1で指摘した縦の間延びは解消
- 1920はコンテンツ1080px中央寄せを維持し、要素の拡大なし。ウォーム紙が左右に広がる設計通りの挙動
- 今集められるフード6カラム・期間限定レール・エリアポスターの縦リズムも自然
- 細部: 左カラム下部にまだ余白があるが、空白が「構図」として読める範囲。許容

## 6. 結論と次のアクション

v1.1のスコープは完遂された。ビジュアル品質は visual-review-home-v1.md の不承認理由をすべて解消している。

リリース前に残るのは1点: **モバイルのsafe-area-inset-top対応（P0）**。修正は小さい（heroの上余白1箇所＋全ページ共通の上部処理の検討）が、検証方法を変える必要がある — **ブラウザスクリーンショットではなくiOSシミュレータ（iPhone 14 Pro系）のスクリーンショットを必須にすること**。あわせてP2の文言2件（0品ラベル・色づく重複）を同梱するのが効率的。

オーナー承認が得られれば、この3点（P0 safe-area＋P2×2）だけに絞った codex-goal-home-v1.2.md を作成する。また、Codexからv1.1の最終報告（lint/build結果・commit hash・Vercel反映）が未提出のため、提出を求める。
