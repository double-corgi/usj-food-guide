# Design Review After Implementation — home-design-v1

レビュー日: 2026-06-12
レビュー担当: Claude（プロダクト責任者 / UX / UIデザイナー）
対象: Codexによる docs/codex-goal-home-v1.md の実装
照合基準: docs/home-design-v1.md / docs/codex-goal-home-v1.md
確認方法: 実コード読解（home-progress-client.tsx 460行 / home-dashboard.tsx / app/page.tsx / globals.css / lib/constants.ts）、変更禁止ファイルのタイムスタンプ確認、スクリーンショット6枚（390/430/768/1280/1920/eaten-390）の目視、generated JSONによるデータ実測。

## 総合判定: **条件付き承認**

設計意図（棚・銘板・刻印ロゴ・大判レール・汎用期間限定枠）は高い精度で実装されており、禁止事項違反はゼロ。ただし**期間限定コレクションの選出結果が「ミニオン」になっており、セクションの意味と矛盾する**（P1）。これと取得演出の発火バグ（P2）を修正してからリリースすべき。

---

## 1. 仕様適合チェック

| 項目 | 判定 | 根拠 |
|---|---|---|
| コレクション棚（2×5 / 2×8 / 3×6） | ✅ | grid-cols-5 → md:8 → lg:6、SHELF_SLOTS 10/16/18。スクリーンショット全幅で確認 |
| 取得済み=フルカラー＋金スタンプ16px | ✅ | h-4 w-4 円形 Gold #fdbb30 ＋ Navy ✓刻印、ring-1 white |
| 未取得=暖色トーンダウン | ✅ | `sepia-[0.18] saturate-[0.56] brightness-[1.06] opacity-90`。グレースケール/黒塗り/ぼかし無し |
| 棚タイル選定（取得済み先頭・最大半分・画像必須・日替わりseed） | ✅ | pickShelfFoods: `slice(0, limit/2)`＋hasDisplayImage＋FNV-1aハッシュの決定的seed（Math.random不使用、Asia/Tokyo日付） |
| 銘板（コレクション数主役・巨大%廃止・4px帯ゲージ） | ✅ | 数字2.35rem（仕様32〜40px内）、%は11px右端、グロー影削除、0%土台あり |
| 非断定語彙 | ✅ | 「/ 販売中 183品（登録分）・残り 183」。「全183品」「全フード」「完全収録」は新規セクションに不存在 |
| ロゴ二層刻印＋左右ゴールド罫 | ✅ | `::before content:attr(data-logo)` Gold 1pxオフセット、`h-px w-5 bg-[#fdbb30]` 左右罫、下線3px廃止、字間0.04em、モバイル1.85rem |
| 今集められるフード（74vw大判・4:3・8件＋すべて見るカード・チップ最大1・写真上バッジ無し） | ✅ | HomeFoodRailCard実装通り。価格未確認除外（hasKnownPriceはpriceSource unknownも除外＝仕様より厳格で良い）。取得済み除外、全取得時の静かな空状態あり |
| 期間限定コレクションの4段フォールバック | ✅(構造) | stage1 eventName/collabグループ(≥3) → stage2 constants定数キーワード → stage3 isLimited系 → stage4 非表示。`is25thFood()` 撤去、25th監査キーワードは `featuredLimitedCollection`（excludeKeywords含む）へ移管済み |
| 「あと◯品」表示＋コンプリートスタンプ | ✅ | remaining計算・complete時Gold表示。「食べた/残り」テキスト区別は廃止され彩色＋スタンプに統一 |
| 最近の記録（3枚＋アルバム導線・記録時のみ） | ✅ | HomeRecentRecords新設、null フォールバック |
| セクション順序 | ✅ | FV→今集められる→期間限定→エリア→最近の記録→店舗→全商品→情報提供（指示書13章と一致） |
| `<main>` 入れ子解消 | ✅ | home-dashboard が div に変更 |
| 装飾削除（光スイープ・放射グラデ・円形ボーダー・drift） | ✅ | globals.cssから該当keyframes消滅。背景はウォーム紙色 #fffdf9 / #fffaf5 |
| reduced-motion | ✅ | `prefers-reduced-motion: reduce` で全アニメ無効化（仕様以上の明示対応） |
| 変更禁止ファイル | ✅ | food-utils.ts / use-food-logs.ts / local-user-data.ts / repositories/foods.ts / tailwind.config.ts / area-overview.tsx すべて改修前のタイムスタンプのまま（6/9以前）。集計定義不変 |
| 集計値の前後一致 | ✅ | 改修前後とも 0/183（スクリーンショット比較）。eaten後ショットで 1/183・残り182 と正しく変動 |
| 禁止物スキャン | ✅ | ロックアイコン・?マーク・COLLECTION英語タグ・AIバナー・合成画像・SaaS統計カード・巨大ヒーロー: grep＋目視でゼロ |
| スクリーンショット | ✅ | 指定6枚すべて screenshots/ に存在 |

## 2. モバイル 390px / 430px

- 破綻なし。横overflowなし、文字切れなし（目視）
- 1画面目: ロゴ＋棚＋銘板がコンパクトに収まり、「今集められるフード」の見出し〜写真上端がチラ見えする構成が成立
- 棚が「自分の図鑑」として最初に目に入る。0品状態でも淡トーンの実写真で埋まり、空の成績表感は解消された
- 大判レールは1枚目がほぼ全幅で表示され、次カードの覗きも機能している
- 下部ナビとの干渉なし（pb-32維持）

## 3. PC 1280px / 1920px

- 1280px: FVが左（ロゴ＋銘板）/右（棚3段）の2カラムになり、スマホ拡大版に見えない。今集められるフードは6カラムグリッド1段 ✅
- 1920px: コンテンツ1080px中央寄せ、要素は引き伸ばされず紙色背景のみ広がる ✅
- 棚タイルサイズも適正でキャップが効いている

## 4. 発見した問題

### P1 — 期間限定コレクションが「ミニオン」を選出している（仕様側の欠陥）

実測: generated JSONのactive商品でeventName/collaborationNameの最多グループは「ミニオン」（生データ60件、表示は公開フィルタ後16件）。次点「マリオ」「25周年」(40件)「ハローキティ」(24件)。

スクリーンショット390/1280でも「期間限定コレクション / ミニオン / あと16品」と表示されている。

問題点:

1. 「ミニオン」はフランチャイズ名であり期間限定イベントではない。常設商品が混ざる可能性が高く、**セクション名「期間限定コレクション」と中身が矛盾**する
2. 「あと16品」は小さなコンプ単位としてやや大きい（本セクションの狙いは「やれそう」感）

これはCodexのミスではなく、私の指示書 stage1（最多グループ採用）の設計穴。修正方針（次回 /goal 用・実装はまだしない）:

- stage1の母集合を「isLimited または saleType が limited/event または 販売終了日あり」の商品に限定してからグループ化する
- またはグループ内の限定商品比率（例: 70%以上）を採用条件に加える
- 採用グループの上限品数（例: 24品）を超える場合は次候補グループへ
- この条件で「25周年」(40件→公開フィルタ後はより小さい)等の真のイベントグループが選ばれるかを実測のうえ確定する

### P2 — スタンプ演出がページ読込のたびに発火する

`useFoodLogs` の logs は非同期（初期 `[]` → setTimeout後に実データ）で供給される。`useNewlyStampedKeys` は初回マウント時に「空集合」を基準として確定するため、**ログ到着時に取得済みタイル全部が「新規取得」と誤判定され、毎回ページ読込時に unlock アニメが再生される**。仕様は「取得直後のみ・1回限り」。

修正方針: `useFoodLogs` の `ready` フラグが true になるまで基準セットを確定しない（ready前のeatenKeys変化はベースライン更新のみ）。

### P2 — unlock アニメの色が旧ミントグリーン

`achievement-unlock` キーフレームのグローが `rgba(137, 232, 191, …)`（緑系）のまま転用されている。real-user-audit-v3で緑系はUSJカラーへ排除済みであり、ここだけ旧配色が復活している。Gold `rgba(253, 187, 48, …)` 系へ変更すべき。

### P3 — 「全商品を見る」の文言リスク

ExploreAllCard（既存流用）の見出し「全商品を見る」は、「全◯品」禁止の趣旨に照らすと「全商品=完全収録」と読まれる余地がある。サブテキストは「図鑑に登録された183種類」と非断定で正しいため、見出しのみ「すべて見る」「登録済みコレクションを見る」等へ寄せるのが安全。

### P3 — デッドコードの残存

`HomeHeaderStats` / `HomeProgressStatusClient` / `StatusMini`（SaaSピル様式の旧UI）がどこからも参照されないままファイル内に残っている。禁止様式のコードが将来再利用される火種になるため削除推奨。あわせて `aria-label="ホーム"` が role の無い div に付いており効果が無い（旧sectionの名残）。

### 未確認事項（Codexの最終報告が必要）

- `npm run lint` / `npm run typecheck` / `npm run build` の実行結果（報告書未受領。本レビューでは未実行）
- git backup/implement コミットとpush、Vercel反映状態
- 期間限定コレクションの動作段の報告（本レビューの実測では stage1「ミニオン」が発火）

## 5. 体験評価（改修前→改修後）

| 観点 | 前 | 後 | 補足 |
|---|---|---|---|
| ユニバ感 | ★★ | ★★★☆ | ウォーム紙色＋Gold刻印で温度が出た。エリアポスターとの接続も自然 |
| コレクション感 | ★★ | ★★★★ | 棚が機能している。0品でも「これから色づく」が直感できる |
| ゲーム感 | ★★ | ★★★☆ | 彩色＋スタンプ＋日替わりで構造はできた。P2修正で演出の信頼性が上がる |
| 高級感 | ★★★ | ★★★★ | 巨大%と光スイープの廃止が効いた。大きい数字が1つだけの規律も維持 |
| コンプ欲 | ★★ | ★★★ | 「あと◯品」は出たが、P1（ミニオン16品）の単位設計が決まれば★4に届く |
| 何度も開きたくなる | ★★ | ★★★☆ | 日替わりseed＋棚の成長＋残り日数チップ。再訪差分は構造的に確保された |

## 6. 結論

リリース前必須: **P1（期間限定コレクションの選出条件）と P2×2（スタンプ発火バグ・緑グロー）の修正**。P3は同梱推奨。修正は小規模（home-progress-client.tsx と globals.css に閉じる）であり、集計ロジック・データ層への影響はない。

オーナー承認が得られれば、上記修正のみを対象とした codex-goal-home-v1.1.md（修正版 /goal）を作成する。本レビューでは実装・コード変更・git操作は行っていない。
