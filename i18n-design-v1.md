# i18n設計書 v1 — ユニコレ多言語対応

作成日: 2026-06-14
担当: Claude（プロダクト責任者 / UX / UIデザイナー / レビュー担当）
種別: 設計のみ。コード変更・git操作・実装は行っていない。

確認対象: package.json（Next.js App Router + TypeScript + Tailwind構成、generated JSON + Supabaseのリポジトリ層）、`app/`（about/areas/eaten/foods/genres/shops/stores/settings等）、`components/`（app-header.tsx・app-footer.tsx・各ページコンポーネント）、`lib/constants.ts`（`appBrand`・`categoryLabels`・`shopTypeLabels`・`diningTypeLabels`・`statusLabels`等の既存ラベル辞書）、`docs/`（home v1.2・area-detail-v1.1までの設計史）。

---

## 1. Objective

ユニコレを日本語話者以外（en / ko / zh-TW）のユーザーにも使いやすくするため、**UI文言を中心とした多言語対応**の設計方針を定める。

今回のゴールはあくまで「設計」であり、以下は対象外:

- 実装（コード変更・辞書ファイル作成・ライブラリ導入）
- URL構造の変更そのものの実行
- 商品名・店舗名・エリア名の翻訳実行

これまでの home v1.2 / area-detail-v1.1 で確立した「ユニバ感・コレクション欲・ゲーム感・高級感」というビジュアル言語と、「販売中◯品（登録分）」のような非確定的な語彙ポリシーは、**多言語化後も全言語で維持されるべき**という前提のもとで設計する。

---

## 2. Supported Languages

| コード | 言語 | 表記方向 | 備考 |
|---|---|---|---|
| `ja` | 日本語 | LTR | デフォルト・フォールバック言語。現状すべての文言の原本 |
| `en` | English | LTR | 海外一般ユーザー向け |
| `ko` | 한국어 | LTR | 韓国からの訪日ユーザー向け（USJ来園者の主要層の一つ） |
| `zh-TW` | 繁體中文 | LTR | 台湾・香港からの訪日ユーザー向け |

- `zh-CN`（簡体字）は今回のスコープ外。将来追加する場合は `zh-TW` の辞書をベースに簡体字変換で対応できる構造にしておくと拡張が容易。
- `ja` を常にフォールバック言語とする（4節で詳述）。

---

## 3. Translation Scope

### 3.1 翻訳対象とするUI文言

以下のカテゴリは**全言語で翻訳対象**とする。

- **ナビ**: ヘッダー・フッターのリンク文言（「ホーム」「探す」「食べた」「エリア」「店舗」など）
- **見出し**: 各ページの `h1`/`h2`（「エリアから探す」「まず食べたい3品」「残りのフード」「販売場所」など）
- **ボタン・リンク文言**: 「すべて見る」「残りをすべて見る」「探す」「情報提供する」など
- **フィルター・ソートのラベル**: `/foods` の絞り込みUI、`sale=active`・`sort=uneaten` 等に対応するラベル文言
- **状態表示**: 「販売中」「販売終了」「期間限定」「コンプリート」「コンプ率◯%」などの定型語
- **空状態の文言**: 「このエリアの販売中フードは現在確認中です」「このエリアに販売終了フードはありません。」「このエリアの1品目を見つけよう。」など
- **エラー文**: `error.tsx`・`global-error.tsx`・`not-found.tsx` の文言、フォーム送信エラーなど
- **説明文・補足文**: 「残りフードをエリアごとに確認できます。」のようなリード文、`unofficialNotice`（USJ非公式である旨の注記）
- **店舗・エリアまわりの定型ラベル**: `shopTypeLabels`（レストラン/フードカート/ワゴン/未分類）、`diningTypeLabels`（テイクアウト/店内飲食/カート販売など）、`categoryLabels`（チュリトス/ポップコーン/ドリンク等のカテゴリ名）、`statusLabels`（公開情報確認中/復刻待ち/終了など）

これらは現在 `lib/constants.ts` に日本語の固定文字列として定義されており、構造としては「キー→ラベル」のマップになっているため、多言語化に最も適した形になっている。

### 3.2 翻訳対象外（検討の上、現状はja固定を推奨）の候補

以下は**翻訳しない方が自然、または翻訳の優先度が低い**と考えられる項目。最終判断はOwner確認が必要（10節「Stop and Ask」参照）。

- **商品名**（`food.name`）: USJの公式商品名は日本語の固有名詞・コラボ表記（「フリーレンのビーフプレート」「25周年アニバーサリー・ドリンクカップ」等）が多く、英語圏ユーザーであっても来園時に現地（日本語の看板・レジ表示）で照合する用途が大きい。直訳すると現地で該当商品が見つけられなくなるリスクがある。
  - 代替案: 商品名は`ja`を主表示のまま残し、**必要な言語のみ補助的に英語表記を併記**する、または翻訳しない。
- **店舗名**（`shop.name`）: 「メルズ・ドライブイン」「スタジオ・スターズ・レストラン」等は、USJ内の看板表示・地図上の表記と一致させる必要があるため、基本的に**翻訳しない（日本語表記を正とする）**方が現地での実用性が高い。英語の公式表記がある場合のみ補助表示を検討。
- **エリア名**（`area.name`）: 「ハリウッド・エリア」「ウィザーディング・ワールド・オブ・ハリー・ポッター」等は、`en`圏ではむしろ英語表記（"The Wizarding World of Harry Potter"）の方が自然な場合がある一方、`ko`/`zh-TW`では現地の看板表記に合わせた方が実用的な可能性がある。エリア名は商品名・店舗名より翻訳の価値が比較的高いが、件数が少なく（10エリア程度）手動でのレビューが可能な範囲。

商品名・店舗名・エリア名の翻訳方針は、いずれも**現地での実用性（来園時に現物と一致するか）**と**コレクション体験としての固有名詞の魅力（「フリーレンのビーフプレート」のような名称自体がコレクション欲を喚起する）**のトレードオフであり、Claudeが単独で決めるべき事項ではないため10節で確認を求める。

---

## 4. Language Switcher Design

### 4.1 配置候補の評価

| 候補 | 評価 | コメント |
|---|---|---|
| 設定ページ（`/settings`） | ◎ 推奨 | 既存の「設定」ページが存在し、`settings-data-panel.tsx` 等の管理系UIが既に置かれている。言語切り替えをここに置くのは「整理する場所」としての設定ページの役割と合致し、ホームのファーストビューやヘッダーの密度に影響しない |
| フッター（`app-footer.tsx`） | ○ 補助候補 | 現状フッターには「アプリについて」「フード一覧」「エリア」「店舗」「食べた記録」「プライバシー」等のリンク群がある。ここに言語切り替えを追加するのは大きな破壊を伴わないが、スクロール最下部のため発見性が低い |
| ヘッダー内（`app-header.tsx`） | △ 慎重 | 「禁止: ヘッダーを重くする」に該当しやすい。地球儀アイコン＋現在言語コードの小さいボタン程度なら検討余地はあるが、PC/モバイル両方でのレイアウト確認が必要 |
| メニュー内（ハンバーガー等） | — | ユニコレには現状グローバルなハンバーガーメニューは存在しない（下部ナビ＋PCヘッダーのリンク構成）。新設は「下部ナビを増やす」「ヘッダーを重くする」の禁止に抵触しやすいため非推奨 |
| 初回アクセス時の小さな選択（言語選択モーダル/バナー） | △ 補助候補 | 初回訪問時にブラウザの`Accept-Language`から推定した言語を軽く提示するのは有効だが、「ホームのファーストビューを壊す」リスクが高いため、モーダルではなく**控えめな1行バナー**程度に限定すべき |

### 4.2 推奨案

1. **主導線**: `/settings` ページ内に「言語 / Language」セクションを新設し、ja / en / ko / zh-TW をリスト選択（現在のラジオ風UIや`settings-data-panel.tsx`のトーンに合わせる）。
2. **補助導線**: `app-footer.tsx` のリンク群の末尾（既存リンクと同じテキストサイズ・配色）に「言語 / Language」リンクを1つ追加し、`/settings` の言語セクションへスクロールアンカーで誘導する。フッターの構成自体（リンク数・並び）は変更せず、末尾に1項目追加するのみ。
3. **初回アクセス**: ブラウザの`Accept-Language`ヘッダーから推定した言語が`ja`以外の場合のみ、ホーム下部（ファーストビュー外、例えば「エリア一覧」セクションの手前など）に「This app is also available in English. / 言語を変更する」という1行の控えめなテキストリンクを表示する案を検討候補とする（採用するかはPhase設計時に再検討）。

### 4.3 禁止事項との整合性

- ホームのファーストビュー（ロゴ・コレクション数・棚タイル）には言語切り替えUIを置かない。
- 下部ナビ（ホーム/探す/食べた/エリア/店舗の5項目）は変更しない。新規タブは追加しない。
- ヘッダーには新規の常時表示UIを追加しない（Phase 1ではヘッダー無変更を基本とする）。

---

## 5. URL Strategy

### 案A: localStorageで言語保存（URL変更なし）

- 概要: `/areas/area-olb56e` のようなURLは言語に関わらず共通。選択した言語は `localStorage`（または将来的にCookie）に保存し、クライアント側で表示言語を切り替える。
- メリット:
  - 既存の全URL・`sitemap.ts`・`robots.ts`・`generateStaticParams`・canonical設定・OG設定に変更が不要。area-detail-v1.1までの実装・レビュー資産がそのまま活きる。
  - 実装範囲が小さく、Phase 1（基盤＋ナビ）から段階導入しやすい。
  - localStorageは既に `lib/local-user-data.ts` / `lib/use-food-logs.ts` で「食べた記録」に使われており、技術的な扱いに慣れている。
- デメリット:
  - SSR/SSGページ（`revalidate`付きの静的生成ページが多い）では、初回HTMLは常に`ja`で生成され、クライアントで言語が切り替わるまで一瞬日本語が見える（FOUC的な現象）の可能性がある。
  - 検索エンジンは基本的に`ja`版のみをインデックスする。多言語SEOのメリットを得られない。
  - 言語ごとのURLを共有・ブックマークできない（「英語版のこのページをシェアしたい」というニーズに弱い）。

### 案B: URLに言語を含める（`/ja`, `/en`, `/ko`, `/zh-TW`）

- メリット:
  - Next.js App RouterのI18n routing（`[locale]`セグメント）の標準パターンに乗れる。`generateStaticParams`で言語×ページの組み合わせを静的生成でき、SSRでも初回から正しい言語のHTMLが返る。
  - 言語別のcanonical・`hreflang`・サイトマップを作れ、多言語SEOに強い。
  - URLで言語が確定するため、シェア・ブックマークが言語別に機能する。
- デメリット:
  - **全ページのルーティング構造に影響する大改修**になる。`app/`配下のほぼ全ディレクトリを`[locale]`配下に移動する必要があり、`generateStaticParams`・`sitemap.ts`・`robots.ts`・`metadata`・既存リンク（`Link href="/areas/..."`等多数）をすべて見直す必要がある。
  - これまでhome v1.2やarea-detail-v1.1で積み上げてきたレイアウト・safe-area対応・既存テストやレビュー済みコードへの影響範囲が非常に大きく、リグレッションリスクが高い。
  - 既存のVercelデプロイ・OG設定・`NEXT_PUBLIC_SITE_URL`等の設定見直しも必要。

### 案C: 最初はlocalStorageで始め、将来URL対応する（段階移行）

- 概要: Phase 1〜4は案Aで進め、UI文言の辞書化・翻訳カバレッジが一定水準に達した段階で、必要であれば`/en`等のURL対応へ移行する。
- メリット:
  - 初期投資が小さく、翻訳の質・カバレッジを先に検証できる。辞書ファイル（5節）さえ言語コードをキーにした構造にしておけば、後からURL方式（案B）に移行する際も辞書自体の再設計は不要（ルーティング層の追加のみで済む）。
  - 「全ページに影響する」という最大リスク（8節）を初期段階では避けられる。
- デメリット:
  - 案Aの欠点（SEO・FOUC・シェア不可）は移行までの期間中残る。
  - 「将来URL対応する」と決めておかないと、辞書の構造設計が場当たり的になり、後から移行コストが増える可能性がある（=今のうちに辞書のキー構造だけは「将来`[locale]`化される前提」で設計しておく必要がある）。

### 比較まとめと方向性（仮）

| 観点 | 案A | 案B | 案C |
|---|---|---|---|
| 初期実装コスト | 低 | 高 | 低（Aと同等） |
| 既存ページへの影響 | 小 | 全ページ大改修 | 小（当面） |
| SEO（多言語インデックス） | 弱い | 強い | 当面弱い、将来強化可 |
| シェア/ブックマークの言語固定 | 不可 | 可能 | 当面不可 |
| リグレッションリスク | 低 | 非常に高い | 低 |

Claudeとしては**案C（まずlocalStorageで開始し、辞書構造だけは将来のURL化を見据えて設計する）**が、現状のプロジェクト規模・これまでの段階的改修（home v1.2 → area-detail-v1.1のように小さく安全に進める方針）と最も整合すると考える。ただし、**URLを将来的に`/en`等へ変更するかどうかはオーナー判断事項**であり、本書では方向性の提案のみとし、確定は10節のStop and Askに含める。

---

## 6. Data Strategy

### 6.1 UI文言の辞書ファイル

- 言語ごとに1ファイル（例: `locales/ja.json` / `locales/en.json` / `locales/ko.json` / `locales/zh-TW.json`）を持つ構造を推奨。
- キーは機能・コンポーネント単位の名前空間で階層化する（例: `nav.home` / `area.remainingCount` / `food.statusEnded` / `common.viewAll`）。
- `lib/constants.ts` の既存ラベル辞書（`categoryLabels`・`shopTypeLabels`・`diningTypeLabels`・`statusLabels`、`appBrand`、`unofficialNotice`）は、**そのままi18n辞書の`ja`版の初期データとして転記できる**構造になっている。Phase 1ではこれらのRecord型をi18nキー化することから始めるのが効率的。

### 6.2 商品名・店舗名・エリア名

- 3.2節で述べたとおり、いずれも**現状は`ja`固定（翻訳しない）を初期案として推奨**。
- 将来翻訳する場合も、商品データ（`food.name`等）に`nameEn`/`nameKo`/`nameZhTw`のような追加フィールドを持たせる形が想定されるが、これは生成JSON・Supabaseスキーマの変更を伴うため、**今回のi18n基盤設計のスコープ外**とする。
- エリア名のみ、件数が少ない（10エリア）ため、UI文言辞書の中に「エリア名翻訳テーブル」として手動で持つ案も検討可能（データソース自体は変更せず、表示用の対訳テーブルをUI辞書側に置く）。ただし採用は10節で確認。

### 6.3 価格・日付の表記

- 価格: 現状「￥2,500」のような日本円表記（`formatFoodPrice`）。USJは日本国内施設であり、購入は常に日本円で行われるため、**通貨は`¥`表記のまま、全言語で変更しない**ことを推奨（参考として外貨換算を表示するのはスコープ外）。
- 日付: 「販売終了日」「残り◯日」等の表記は、`en`/`ko`/`zh-TW`では日付の語順・表現が異なる（例: 「残り3日」→ "3 days left" / "3일 남음" / "剩餘3天"）。日付フォーマット自体（西暦表記）は変更不要だが、**「残り◯日」のような合成文は言語ごとに語順を変えられるテンプレート形式**（例: `{n}` プレースホルダーを使う）で辞書化する必要がある。

### 6.4 「限定」「販売中」「販売終了」等の状態語

- `statusLabels`（公開情報確認中/復刻待ち/終了/非表示/公式情報要確認）や、area-detail-v1.1で整理した「販売中」「販売終了」「期間限定」「コンプリート」は、いずれもUI文言として全言語で翻訳対象。
- これまでのレビューで確立した「断定しない語彙ポリシー」（例:「販売中 37品（登録分）」、「全183品」のような確定表現は使わない）は、**翻訳後も同じ非確定的なニュアンスを保つ**必要がある。直訳すると英語では曖昧さが伝わりにくい場合があるため、Phase 2以降の翻訳時に文言レベルでのレビューが必要（4言語間で「確定っぽさ」のレベルが揃っているかを見る）。

### 6.5 フォールバック言語

- `ja`を常にフォールバック言語とする。
- 辞書に該当キーの翻訳が存在しない場合、`en`/`ko`/`zh-TW`のいずれでも`ja`の文言を表示する（「翻訳の取りこぼしで空文字や`undefined`が表示される」事態を避ける）。
- 将来的に`en`の翻訳カバレッジが先行する場合は、`ko`/`zh-TW`の二次フォールバックを`en`にする案もあるが、初期は「全言語→`ja`」のシンプルな1段フォールバックを推奨。

---

## 7. File Impact（Codexが触る可能性のあるファイル候補）

### Phase 1（i18n基盤＋ナビ）で触れる可能性が高いファイル

- 新規: `lib/i18n/`（辞書ファイル群、言語判定・取得用のユーティリティ）
- 新規または変更: `lib/use-locale.ts`（仮称、選択言語の取得・保存。`use-food-logs.ts`のlocalStorage活用パターンを参考にできる）
- `components/app-header.tsx`（ナビ文言の差し替え。レイアウト変更は最小限）
- `components/app-footer.tsx`（ナビ・補助リンク文言の差し替え、言語切り替えへの導線追加）
- `app/settings/page.tsx`（言語切り替えUIの追加）
- `lib/constants.ts`（既存ラベル辞書をi18nキー化、または辞書から参照する形に変更）
- `app/layout.tsx`（`<html lang="...">`の動的化、`metadata`の言語別対応の検討）

### Phase 2以降で触れる可能性があるファイル

- `app/page.tsx`・`components/home-dashboard.tsx`・`components/home-progress-client.tsx`（ホーム文言）
- `app/foods/page.tsx`・`app/foods/[id]/page.tsx`・`components/food-grid.tsx`・`components/food-card.tsx`・`components/food-detail.tsx`（探す・商品詳細の文言、フィルター・ソートラベル）
- `app/eaten/`・`components/eaten-*.tsx`（食べた記録関連の文言）
- `app/areas/`・`components/area-*.tsx`（エリア関連、area-detail-v1.1で整理済みの文言群）
- `app/stores/`・`components/store-*.tsx`（店舗関連）
- `app/genres/`・`app/shops/`（カテゴリ・店舗種別の文言）

### 原則触らない想定のファイル

- `lib/repositories/`、`lib/food-utils.ts`、`lib/use-food-logs.ts`、`lib/local-user-data.ts`、`scripts/`、`supabase/`（データ層・スキーマは今回のi18n基盤設計のスコープ外）
- 商品・店舗・エリアの生成JSONそのもの（名称翻訳をスコープに含めない限り）

---

## 8. Implementation Phases

一気に全ページを翻訳せず、以下の段階で進める。各Phaseの完了時にCodex実装→Claudeレビューのサイクルを挟む想定（これまでのhome/area-detail改修と同じ進め方）。

### Phase 1: i18n基盤とナビ

- 辞書ファイル構造の確立（`ja`を基準に4言語分の枠を用意。`en`/`ko`/`zh-TW`は最初は主要なナビ・共通文言のみ翻訳済みでよい）
- 言語選択の保存・取得（案AのlocalStorage方式）
- `app-header.tsx` / `app-footer.tsx` のナビ文言の翻訳
- `/settings` への言語切り替えUI追加
- `<html lang>` の動的化

### Phase 2: ホーム

- `app/page.tsx` / `home-dashboard.tsx` / `home-progress-client.tsx` の見出し・説明文・状態表示（「販売中◯品（登録分）」「期間限定コレクション」「あと◯品」等）

### Phase 3: 探す・商品詳細

- `/foods` のフィルター・ソートUI、空状態・件数表示
- `/foods/[id]` の見出し・状態ラベル・関連セクション文言
- 商品名自体は3.2節の方針（当面`ja`固定）に従う

### Phase 4: 食べた・エリア・店舗

- `/eaten` の見出し・空状態・進捗表示
- `/areas`・`/areas/[id]`（area-detail-v1.1で整理済みの文言群一式）
- `/stores`・`/stores/[id]`
- エリア名・店舗名の翻訳方針（3.2節）が確定していればここで反映

### Phase 5: ストア提出用文言

- PWAマニフェスト（`manifest.webmanifest`）・`metadata`（`appBrand`の多言語化）
- About/プライバシー/利用規約/免責事項などの静的ページ（`app/about`・`app/privacy`・`app/terms`・`app/disclaimer`等）
- App Store / Google Play 提出用のストア説明文・スクリーンショット文言（アプリ内UIではないが、ストア掲載言語との整合が必要）

---

## 9. Risks

- **全ページに影響する**: i18n基盤（Phase 1）はアプリ全体のレイアウト・ヘッダー/フッター・`layout.tsx`に触れるため、これまでのページ単位の小修正（area-detail-v1.1等）より影響範囲が広い。Phase分割と、Phase 1完了時点での全ページ目視確認が重要。
- **文言の取りこぼし**: 4言語×多数のキーになるため、未翻訳キーが`ja`のまま表示される、あるいは空文字になるリスク。フォールバック設計（6.5節）と、Phase完了時のgrepベースのチェック（「翻訳キーが存在するが訳文が空」のリスト化）が必要。
- **商品名翻訳の不自然さ**: 3.2節で述べたとおり、USJの公式コラボ商品名を直訳すると、現地での視認性・コレクション欲（固有名詞としての魅力）を損なう可能性がある。翻訳するかどうかはStop and Ask対象。
- **SEO**: 案A（localStorage）採用時は多言語ページが検索エンジンにインデックスされず、海外ユーザーの検索流入が見込めない。案Cで将来URL化する場合も、移行時に既存`ja`ページのSEO評価（被リンク等）への影響を考慮する必要がある。
- **URL設計**: 5節のとおり、案Bは全ページ大改修になり、これまでの段階的改修の方針と相性が悪い。案Cを選ぶ場合も「将来URL化する」という前提を辞書構造に反映しておかないと、後から構造変更コストが増える。
- **localStorageとの相性**: 既に「食べた記録」がlocalStorageを使っているため、言語設定も同様の仕組みに乗せやすい一方、**ユーザーがブラウザのデータを消去すると言語設定もリセットされる**。また、サーバーコンポーネントで生成される初期HTMLは言語設定を認識できないため、初回表示は常に`ja`になる（4.1節の初回アクセス導線・FOUC対策が必要）。
- **App Store / Google Playの言語設定**: PWA/ネイティブラッパーとして配信する場合、ストアの「対応言語」設定とアプリ内で選択可能な言語が一致しているか、ストアの説明文・スクリーンショットも対象言語分用意されているかの確認が必要（Phase 5）。
- **既存デザインが崩れるリスク**: `en`/`ko`/`zh-TW`は`ja`より文字幅・文字数が変わる（特に`en`は文章が長くなりやすい）。home v1.2・area-detail-v1.1で確立した「銘板」「カードレス構成」「行リスト」等のレイアウトは、文字数が増えた際にline-clamp・truncate・折り返しが崩れないか、Phase 2以降で言語別のスクリーンショット確認が必須。

---

## 10. Stop and Ask Questions

以下はClaudeが単独で決めず、オーナー（U様）の判断を確認する。

1. **URLを`/en`等に変えるか**: 5節の案A/B/Cのうち、最終的にどの方針で進めるか。Claudeは「まず案A、将来案Cの形で必要なら案Bに移行」を提案するが、最初から案Bを志向するかどうかはオーナー判断。
2. **商品名を翻訳するか**: `food.name`を多言語化するか、`ja`固定とするか。翻訳する場合、データフィールド追加（生成JSON/Supabaseスキーマ変更）が必要になり、スコープが大きく変わる。
3. **エリア名を翻訳するか**: `area.name`を多言語化するか。件数が少ないため対訳テーブル方式は技術的に小規模だが、翻訳するかどうかの方針確認が必要。
4. **店舗名を翻訳するか**: `shop.name`を多言語化するか。現地の看板表記との整合性が論点。
5. **自動翻訳を使うか**: 辞書の初期翻訳を自動翻訳（機械翻訳）で生成し、後でレビュー・修正するか、最初から人力翻訳のみとするか。
6. **外部翻訳APIを使うか**: 実行時翻訳（外部APIをアプリから呼ぶ方式）を使うか、ビルド時に確定した静的辞書のみを使うか。外部APIを使う場合、レイテンシ・コスト・オフライン時の挙動・利用規約上の制約（USJ非公式アプリとしての立ち位置）も論点になる。

---

## 11. Verification Plan（将来のCodex実装後を想定した検証観点の整理）

本書はまだ`/goal`を作らないが、将来Phase 1の`/goal`を作成する際に検証要件として組み込むことを想定し、観点のみ整理する。

- Phase 1完了後:
  - `lint` / `typecheck` / `build`が成功すること
  - `ja`/`en`/`ko`/`zh-TW`それぞれで`/settings`から言語切り替えができ、ヘッダー・フッターのナビ文言が切り替わること
  - 言語切り替え後、ページ遷移・リロードしても選択言語が保持されること（localStorage）
  - `ja`未対応キーがない（フォールバックが機能している）こと
  - ホームのファーストビュー・下部ナビ・ヘッダーのレイアウトが4言語すべてで崩れていないこと（5幅 × 4言語のスクリーンショット確認）
- Phase 2以降:
  - 各Phase対象ページについて、4言語でのスクリーンショット確認（特に`en`での文字数増加によるレイアウト崩れの有無）
  - 「販売中◯品（登録分）」のような非確定的な語彙ポリシーが、翻訳後も同じニュアンスで維持されているかの文言レビュー
  - 商品名・店舗名・エリア名（翻訳しない方針の場合）が、`ja`のまま正しく表示され続けていること

---

## 12. 結論

ユニコレの多言語対応は、(1) UI文言を中心に`lib/constants.ts`の既存ラベル辞書を起点としたi18n辞書化から始め、(2) URLは当面localStorage方式（案A）とし将来のURL化（案C）に備えた辞書構造にし、(3) 言語切り替えは`/settings`を主導線・フッターを補助導線とし、ホームのファーストビュー・下部ナビ・ヘッダーは変更しない、という方針が現状の段階的改修プロセスと最も整合する。

商品名・店舗名・エリア名の翻訳方針、URL方式の最終確定、自動翻訳/外部API利用の有無はオーナー判断が必要なため、10節の確認を経てからPhase 1の`/goal`作成に進む。
