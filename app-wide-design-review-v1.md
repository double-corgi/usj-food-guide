# App-Wide Design Review v1 — ユニコレ全体監査

レビュー日: 2026-06-12
レビュー担当: Claude（プロダクト責任者 / UX / UIデザイナー）
対象: `/` `/foods` `/foods/[id]` `/eaten` `/areas` `/areas/[id]` `/stores` `/stores/[id]` の8ページ
確認方法: 全ページのスクリーンショット目視（390px系・1280px系・iOS Simulator系）、エリア詳細(`app/areas/[id]/page.tsx` 全文)・`area-eaten-foods.tsx`・`area-food-status-lists.tsx`・`food-grid.tsx`・`eaten-experience.tsx` 等の実コード照合、禁止語・英語装飾ラベルの全体grep。

本レビューは分析のみ。実装・コード変更・git操作は行っていない。改善実装の指示書（/goal）もまだ作らない。

---

## 1. 総合判定: **条件付き承認**

アプリ全体は公開に耐える品質に達している。致命的なバグや断定表現（「全商品」「全フード」「完全収録」はgrepで全ページ不検出）はない。

ただし、ホームだけが v1.2 世代（ウォーム紙・棚・カードレス・Gold/Navy）に進化した結果、**アプリの中に2つのデザイン世代が同居している**。他ページはクールグレー背景＋白カード＋shadow＋青アクセントの旧世代のままで、ホームから遷移した瞬間に「別のアプリに飛んだ」感覚が生まれる。特に `/areas/[id]`（エリア詳細）は、このプロジェクトが禁止してきた「統計カード4連発・管理画面風・装飾英語」が密集しており、最も古い顔のまま残っている。

条件: P1群（エリア詳細・デザイン世代統一・/eatenの0品状態）の改修計画を立てること。

## 2. ページ別点数（100点満点）

| ページ | 点数 | 一言評価 |
|---|---|---|
| ホーム `/` | 88 | v1.2承認済み。棚・銘板・大判レールが機能。アプリの基準点 |
| 探す `/foods` | 64 | 写真と規律は良いが、黒い「食べた」ボタンが全カードで連打され、絵文字チップ・クール背景がホームと断絶 |
| 商品詳細 `/foods/[id]` | 74 | 大きな写真・図鑑コード・食べたボタンの流れは良い。8ページ中ホームに次ぐ完成度 |
| 食べた `/eaten` | 60 | 記録がある人にはアルバムだが、0品ユーザーには「0%の壁」（ゼロ4連＋空ボックス2つ＋0%行×10） |
| エリア `/areas`＋`/areas/[id]` | 58 | 一覧は78点（ポスターが美しい）、詳細は42点（紺色ダッシュボード）。落差が最大の問題 |
| 店舗 `/stores`＋`/stores/[id]` | 64 | 詳細は店舗写真＋買える商品で70点。一覧は単調な白リストで60点、「エリア確認中」が見出しに露出 |

## 3. 優先修正順位

### P0: なし

リリースを止める欠陥はない。以下P1は「品質の溝」であり、計画的に直せばよい。

### P1

1. **`/areas/[id]` が旧世代ダッシュボードのまま**（最重要）
   - 紺色ヒーローに「フード37 / 販売場所15 / 期間限定4 / カート販売7」の**統計カード4連発**（USJ_FOOD_GUIDE_DESIGN.mdの明示的禁止事項）
   - **エリア写真が無い**。`/areas` 一覧の美しいポスターをタップすると、写真ゼロの紺のパネルに着地する（期待の裏切り）
   - 販売場所がグレーのチップ15個以上の壁
   - **装飾英語ラベルが3種残存**: 「Area Memory」「Missing Foods」「Archive Foods」（uppercase tracking付き。設計ルールの禁止事項）
   - 「注目フード3品」に#1/#2/#3のランキングバッジ（PRODUCT.md「根拠の弱いランキングを主役にしない」と緊張関係）
   - 白カード＋グラデ販促カード＋FoodGridの縦積みで、1ページに3世代の様式が混在
2. **デザイン世代の分裂（全ページ共通）**
   - ホーム: ウォーム紙 #fffaf5 / カードレス / Gold＋Navy / 絵文字なし
   - 他ページ: クールグレー #f8fafc / 白カード＋shadow-soft / 青(park)アクセント一色 / 絵文字チップ
   - 遷移のたびに温度が変わる。「ユニコレらしさ」が現状ホームにしか無い
3. **`/eaten` の0品状態が「ゼロの壁」**
   - 上部に「食べた 0品 / 販売中コンプ 0% / 図鑑 0% / 総額 未記録」の4連ゼロ
   - 空の点線ボックスが2つ
   - その下に「エリアごとの記録」として **0%の白い統計カードが10行**（1280pxでは grid 配置でSaaSダッシュボードそのもの）
   - ホームv1.2が確立した空状態の作法（ゼロを突きつけない・誘い文言）が適用されていない

### P2

1. **`/foods` の管理感の残り**: 全カード下部に黒い「食べた」ピルが並び、画面内に同じ黒ボタンが10個以上（ボタンの壁）。カテゴリチップのOS絵文字（🍕🍔🍿）。ヘッダー下の「183品 図鑑184品」カウンタ。フィルタ自体はチップ＋折りたたみで許容範囲
2. **`/stores` 一覧**: 「エリア確認中」というデータ品質語がエリア見出しとしてユーザーに露出。白カードの単調な縦リスト。「フードカート」「低アレルゲンメニュー取扱レストラン」という総称が固有店舗と同格に並ぶ
3. **未確認系文言の露出継続**: 「価格未確認」「エリア確認中」等が food-card / food-grid / food-detail / eaten-experience の4ファイルに残存（ホーム新セクションからは排除済み。一覧・詳細では情報としての価値もあるため、見せ方の整理が必要）
4. **販売終了商品の grayscale**: food-card は ended 商品を `opacity-75 grayscale` で沈めており、v1.1で確立した「沈めずに差を作る」方針と矛盾

### P3

1. ナビ外の孤児ルート: `/genres` `/shops` がページとして存在するが下部ナビからも主要導線からも到達しにくい（役割の重複整理が必要）
2. 商品詳細のカテゴリ表記ゆれ: プレート商品に「麺・パスタ」等、データ由来の違和感（UI課題ではなくデータ課題）
3. `/eaten` のタブ「次回食べたい」がある場合だけ出る不意打ち的なUI（機能は残す方針のため、見せ方のみ将来検討）
4. 店舗詳細のサムネ角丸・余白がホームの棚と微妙に異なる（統一トークン化の際にまとめて）

## 4. 良い点

- **写真規律は全ページで守られている**: 比率統一・名前行数固定・バッジ最大2の規律が `/foods` でも維持され、写真の質が高い
- **`/areas` 一覧のポスターカード**はホームと並ぶ完成度で、すでに「ユニコレらしい」
- **商品詳細の構成**（図鑑コード→大判写真→名前→価格→食べた→販売場所→レビュー→折りたたみ確認情報）は設計書通りで、縦の階層が正しい
- **危険な断定表現ゼロ**: 「全商品/全フード/完全収録」は全ページから排除済み。「販売中◯品（登録分）」「登録済みコレクション」の語彙が定着
- **下部ナビ**: ホーム/探す/食べた/エリア/店舗の5項目は順番・役割とも自然。重複導線も許容範囲（ホーム下部の補助導線はナビの再掲として機能）
- safe-area対応は全ページでSimulator実証済み（v1.2）

## 5. 悪い点

- ホームだけが新世代で、**遷移した瞬間に世界観の温度が落ちる**（紙→グレー、棚→白カード、Gold→青一色）
- `/areas/[id]` は禁止事項（統計4連発・管理画面風・装飾英語・ランキングバッジ）の集積地になっている
- `/eaten` の初回体験がホームv1.2と真逆（ゼロを4回突きつける）
- `/foods` の黒ボタン連打が「図鑑を眺める」気分を「処理する」気分にしている
- 取得済み表現がページごとにバラバラ: ホーム=Gold枠＋スタンプ、/foods=青ボタン化、/eaten=なし。「集めた印」の統一言語が無い

## 6. 最初に直すべきページ

**`/areas/[id]`（エリア詳細）。**

理由: (1) 禁止事項違反が最も密集している、(2) `/areas` 一覧のポスター→詳細という動線は「ユニバに行きたくなる」の中核なのに、着地先が最も管理画面的、(3) ホームの部品（ポスター画像・棚の取得表現・あと◯品）を流用できるため、改修コストに対する世界観の回復が最大。

次点は `/eaten` の0品状態（ホームv1.2の空状態作法の移植で小さく直る）。

## 7. 触らない方がいいページ

- **ホーム `/`**: v1.2で承認済み。完成形として全ページの基準にする。触らない
- **`/areas` 一覧**: ポスターカードは完成している。詳細との接続のために文言1行触る程度に留める
- **商品詳細 `/foods/[id]`**: 構成は正しい。背景色などの世代統一に乗せる以外、構造をいじらない
- **店舗詳細 `/stores/[id]`**: 「この店舗で買える商品」中心の構成は機能している。優先度を上げない

## 8. Codex実装前に確認すべき質問（オーナー判断）

1. **世代統一の方向**: ウォーム紙背景（#fffdf9/#fffaf5）とカードレス様式を全ページへ展開してよいか。それとも「ホームだけ特別な顔」を意図的に残すか（推奨: 全ページ展開。ただし一括ではなくページ単位で段階実施）
2. **エリア詳細のヒーロー**: `/areas` 一覧と同じ公式エリア画像（area-overview.tsxにハードコード済みの10枚）をエリア詳細のヒーローに再利用してよいか（画像利用ポリシーの確認）
3. **エリア詳細の統計4項目**: 「フード/販売場所/期間限定/カート販売」の数字カードは全廃してよいか。残すなら何を1つだけ残すか（推奨: 「残り◯品」のみ）
4. **「注目フード3品」のランキングバッジ**: #1/#2/#3表記をやめ「まずはこれ」的な非順位表現に変えてよいか
5. **/eaten 0品状態**: エリアごとの0%カード10行を、記録が生まれるまで非表示（または大幅縮小）にしてよいか
6. **/foods の「食べた」ボタン**: カード上の黒ピルを、ホームの取得表現（Goldスタンプ系）に寄せた控えめなトグルへ変えてよいか。誤タップ防止の観点も含めて方針確認
7. **絵文字チップ**: カテゴリ絵文字をやめる場合、置き換えは「文字のみ」で良いか（新規アイコン追加は禁止のまま）
8. **/genres・/shops の扱い**: 残すか、/foodsのフィルタへ統合して廃止するか

## 9. 次に作るべき設計書

1. **`area-detail-design-v1.md`**（最優先）: エリア詳細の再設計。ポスターヒーロー＋「残り◯品」＋このエリアで食べたフード（棚の取得表現を流用）＋未食フード＋終了間近、の構成案。統計4連発・英語ラベル・チップ壁の廃止を明記
2. **`app-unify-design-v1.md`**: 世代統一の共通仕様書。背景トークン・カード様式・取得済み表現（Goldスタンプ）・空状態の作法・絵文字/英語ラベル禁止を「全ページ共通ルール」として1冊に定義し、/eaten 0品状態と /foods の調整をこの中の個別章にする
3. （データ系・別トラック）`data-quality-plan.md`: 「エリア確認中」「価格未確認」露出の根本対応は表示の工夫よりデータ承認作業。非表示候補約75件の承認とあわせて計画化

実装順の提案: ①area-detail（1ページ・効果最大）→ ②eaten 0品状態（小）→ ③foods 調整＋世代統一の横展開。①②は独立して /goal 化できる。

---

本監査をもって現状分析を完了とする。オーナーの方針回答（8章）を受けてから、上記設計書の作成に進む。
