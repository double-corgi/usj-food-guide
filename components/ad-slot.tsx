import type { ReactNode } from "react";

type AdSlotProps = {
  className?: string;
  slotId?: string;
  variant?: "inline" | "fixed";
  children?: ReactNode;
};

export function AdSlot(_props: AdSlotProps) {
  return null;
}
